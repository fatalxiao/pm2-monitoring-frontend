import * as actionTypes from 'reduxes/actionTypes';

const initialState = {};

function nav(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default nav;
